[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run db:generate && npx prisma db push --skip-generate && npm run build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
